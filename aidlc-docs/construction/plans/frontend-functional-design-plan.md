# Functional Design Plan — Unit 2: frontend

## 実行ステップ

- [x] Step 1: コンポーネント構造と責務の定義（FC-01〜FC-10）
- [x] Step 2: ページ・画面フローとナビゲーション設計
- [x] Step 3: 状態管理設計（Jotai atoms + React Query）
- [x] Step 4: フォームバリデーションルール定義（SeedFormPage）
- [x] Step 5: API連携ポイントの整理（各コンポーネント × バックエンドエンドポイント）
- [x] Step 6: Supabase Realtimeイベント設計
- [x] Step 7: エラーハンドリング・ローディング状態の設計
- [x] Step 8: ドメインエンティティ（TypeScript型定義）設計
- [x] Step 9: ビジネスルール・UI制約の定義
- [x] Step 10: アーティファクト生成（business-logic-model.md / business-rules.md / domain-entities.md / frontend-components.md）

---

## 質問

以下の質問に回答してください（[Answer]: タグに記入）。

---

### Q1: Seed フィードのデフォルト表示

SeedFeedPage（FC-02）のデフォルト表示状態を教えてください。

A) すべての Seed を新着順で表示（フィルタなし）
B) ログイン中は自分の Seed を優先表示、非ログインは全件表示
C) 人気順（Log 数が多い順）をデフォルトに
D) その他

[Answer]:A

---

### Q2: SeedFormPage（FC-04）のステップフォーム構成

8タイプ対応のステップ型フォームについて、各ステップの構成を教えてください。

A) タイプ選択 → 共通フィールド（タイトル・本文）→ タグ付け → 確認（固定3〜4ステップ）
B) タイプ選択 → タイプ別専用フィールド → タグ付け → AI 知恵洗浄プレビュー → 確認
C) タイプ選択 → 共通フィールド → タグ付け → AI 知恵洗浄プレビュー → 確認
D) その他（詳細を記入）

[Answer]:A

---

### Q3: AI 知恵洗浄（US-203）のUI体験

本文入力中のデバウンス AI 呼び出しについて、ユーザー体験を教えてください。

A) インライン表示：入力エリアの下にサジェストが随時表示される
B) サイドパネル：画面右側にリアルタイムで洗浄済みテキストが表示される
C) 送信後のみ：投稿ボタン押下後に確認モーダルで表示
D) 任意呼び出し：「AIで整える」ボタンを押した時だけ起動

[Answer]:A

---

### Q4: GrowthIndicator（FC-09）のビジュアル表現

6段階の植物成長ビジュアルについて、表現方法を教えてください。

A) SVGアニメーション（段階に応じてパーツが追加されるアニメ）
B) 静止画アセット6種の切り替え（植物の絵が段階ごとに変わる）
C) CSSプログレスバー＋絵文字（シンプルな実装）
D) テキストラベル＋パーセンテージのみ（UIデザインは後回し）

[Answer]:A

---

### Q5: 通知（FC-10）の既読管理

NotificationDropdown での既読処理タイミングを教えてください。

A) ドロップダウンを開いた時点で全件既読
B) 各通知アイテムをクリックした時点で個別既読
C) 「全て既読にする」ボタンで一括既読
D) BとCの両方（個別クリック + 一括ボタン）

[Answer]:A

---

### Q6: 認証保護ルートの設計

ログインが必要なページへの未ログインアクセス時の挙動を教えてください。

A) ログインページにリダイレクト（戻り先URLをクエリパラメータで保持）
B) ログインモーダルをその場で表示
C) ページは表示するがCTAのみロック（一部機能だけ制限）
D) AとBの組み合わせ（ページによって使い分け）

[Answer]:A

---

### Q7: Louge 詳細（FC-07）の Fork ボタン制御

Fork ボタンの表示・活性化条件を教えてください。

A) ログインユーザー全員に表示・クリックで即フォーム遷移
B) ログインユーザーのみ表示（非ログインは非表示）
C) ログインユーザーに表示するが、すでに同 Louge から Fork 済みの場合はグレーアウト
D) 表示は全員、クリック時にログイン確認モーダルを出す

[Answer]:B

---

### Q8: エラーハンドリングのUI方針

API エラー発生時のフィードバック方法を教えてください。

A) トースト通知（画面右下に一時表示、3秒後自動消滅）
B) インラインエラーメッセージ（フォーム直下・ページ内に表示）
C) エラーページへの遷移（全画面エラー表示）
D) AとBの組み合わせ（フォームはインライン、その他はトースト）

[Answer]:A

---

### Q9: モバイル対応の優先度

フェーズ1のモバイルレスポンシブ対応について教えてください。

A) モバイルファースト（スマホを主要ターゲットとして設計）
B) デスクトップファースト（PCを主要ターゲット、モバイルは後回し）
C) 両対応（主要ページのみレスポンシブ、複雑なページはデスクトップのみ）
D) PWA対応も含めて検討したい

[Answer]:C

---

### Q10: ページネーション vs 無限スクロール

SeedFeedPage（FC-02）・LougeListPage（FC-06）の一覧表示方式を教えてください。

A) 無限スクロール（スクロール下部で自動ロード）
B) ページネーション（1ページN件、ページ切り替えボタン）
C) 「もっと見る」ボタン（手動でロード追加）
D) フィードはA、LougeリストはB（使い分け）

[Answer]:A
